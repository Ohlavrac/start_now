import 'package:flutter/material.dart';

class AppColors {
  static const background = Color(0xFF111C2E);
  static const loadimage = Color(0xFFe6dddc);
  static const colorIcon = Colors.white30;
  static const colorProgress = Colors.red;
  static const colorContainer = Color.fromARGB(192, 255, 255, 255);
  static const colorWhite = Colors.white;
  static const colorBlack = Colors.black;
  static const colorGenereButton = Color.fromARGB(255, 28, 109, 156);
  static const colorCard = Color.fromARGB(226, 250, 242, 242);
}
